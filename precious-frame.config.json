{
  "$comment": "Precious Frame runtime config - reloaded on every run.",
  "judge": {
    "$comment": "qwen uses DASHSCOPE_API_KEY. Missing keys or API errors fall back to local pixel scoring.",
    "provider": "qwen",
    "model": "qwen3-vl-plus",
    "baseUrl": "https://dashscope-us.aliyuncs.com/compatible-mode/v1"
  },
  "loop": {
    "$comment": "bar is the score needed to stop early.",
    "bar": 7.5,
    "maxRounds": 8
  }
}
